# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs
.venv/
venv/

# Build output. The icon is generated by tools/make_icon.py, the binaries by
# PyInstaller - nothing here is source.
build/
dist/
*.spec.bak

# One-off publish helper, not part of the project.
publish.sh

# Player profile. A checkout saves to game/data/; that is local progress, not
# something to ship. Packaged builds save to the user-data dir instead.
game/data/save.json
game/data/save.json.corrupt
